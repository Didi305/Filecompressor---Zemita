{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "clean-build",
      "type": "shell",
      "command": "cmd.exe",
      "args": [
        "/c",
        "if exist build (rmdir /s /q build) && mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Release .. && cmake --build . --config Release"
      ],
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}